# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven and Gradle

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  setup_pipeline:
    name: Setup Pipeline
    #needs: commit_at
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      #### ---- Maven Clean ----
      - name: Maven Clean
        run: mvn clean
      #### ---- Maven Compile ----
      - name: Maven Compile
        run: mvn compile
      #### ---- Gradle Clean ----
      - name: Gradle Clean
        run: sh ./gradlew clean
      #### ---- Gradle Assemble ----
      - name: Gradle Assemble
        run: sh ./gradlew assemble

  test:
    name: Test
    needs: setup_pipeline
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      #### ---- Maven Test ----
      - name: Maven Test
        run: mvn test
      #### ---- Gradle Test ----
      - name: Gradle Test
        run: sh ./gradlew test

  build:
    name: Build
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      #### ---- Maven Build ----
      - name: Maven Package
        run: mvn -B package --file pom.xml
      #### ---- Create 'target/libs' directory to store Artifact Built by Maven ----
      - name: Copy Files for Maven Artifact
        run: mkdir target/libs
      #### ---- Move jars files to 'target/libs' directory ----
      - name: Copy Files for Maven Artifact
        run: cp target/*.jar target/libs
      #### ---- Upload Artifact Built by Maven ----
      - uses: actions/upload-artifact@v1
      - name: Upload Artifact Built by Maven
        with:
          name: JSON-java-maven
          path: target/libs
      #### ---- Gradle Build ----
      - name: Gradle Build
        run: sh ./gradlew build
      #### ---- Upload Artifact Built by Gradle ----
      - uses: actions/upload-artifact@v1
      - name: Upload Artifact Built by Gradle
        with:
          name: JSON-java-gradle
          path: build/libs

 # Deploy Script Comes here
    #- name: Publish to GitHub Packages Apache Maven
    #  run: mvn deploy -s $GITHUB_WORKSPACE/settings.xml
    #  env:
    #    GITHUB_TOKEN: ${{ github.token }}

